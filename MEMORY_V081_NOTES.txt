v0.8.1 memory classified

変更内容:
- 自動メモリ生成を4分類化
  # fact
  # style
  # open_topics
  # avoid
- 毎回追記ではなく、既存メモを整理して最新版として保存する方針に変更
- メモ保存上限を 2200 → 3200 文字へ拡張
- 個人情報・一回だけの冗談・ピット側発言だけの思い込みを保存しにくくした

テスト例:
1. 「チョコより和菓子の方が落ち着くかも」
2. person_memories を確認
3. # fact / # style / # open_topics / # avoid の形式に更新されていれば成功
